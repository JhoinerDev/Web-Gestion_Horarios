import React from 'react';
import { Box, Typography } from '@mui/material';

const SOADetails = () => {
    return (
        <Box>
            <Typography variant="h5">Detalles de SOA</Typography>
            <Typography variant="body1">Información relacionada con la Arquitectura Orientada a Servicios para el sistema de horarios.</Typography>
        </Box>
    );
};
export default SOADetails;