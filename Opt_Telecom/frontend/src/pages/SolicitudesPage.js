import React from 'react';
import { Box, Typography } from '@mui/material';

const SolicitudesPage = () => {
    return (
        <Box>
            <Typography variant="h4">Gestión de Solicitudes de Clase</Typography>
            <Typography variant="body1">Aquí irá el formulario y la tabla de solicitudes.</Typography>
        </Box>
    );
};
export default SolicitudesPage;