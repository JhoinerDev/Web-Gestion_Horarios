import React from 'react';
import { Box, Typography } from '@mui/material';

const RestriccionesPage = () => {
    return (
        <Box>
            <Typography variant="h4">Gestión de Restricciones</Typography>
            <Typography variant="body1">Aquí irá el formulario y la tabla de restricciones.</Typography>
        </Box>
    );
};
export default RestriccionesPage;