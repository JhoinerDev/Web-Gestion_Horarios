import React from 'react';
import { Box, Typography } from '@mui/material';

const MateriasPage = () => {
    return (
        <Box>
            <Typography variant="h4">Gestión de Materias</Typography>
            <Typography variant="body1">Aquí irá el formulario y la tabla de materias.</Typography>
        </Box>
    );
};
export default MateriasPage;