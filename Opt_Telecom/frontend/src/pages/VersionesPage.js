import React from 'react';
import { Box, Typography } from '@mui/material';

const VersionesPage = () => {
    return (
        <Box>
            <Typography variant="h4">Gestión de Versiones de Horario</Typography>
            <Typography variant="body1">Aquí irá la tabla y las acciones para las versiones de horario.</Typography>
        </Box>
    );
};
export default VersionesPage;